{
  "name": "icmal-desktop",
  "version": "1.0.25",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "description": "İcmal masaüstü uygulaması",
  "main": "dist/main.js",
  "author": "İcmal",
  "scripts": {
    "dev": "tsc && concurrently -k -n next,electron -c blue,green \"npm run dev:next\" \"npm run dev:electron\"",
    "dev:next": "cd .. && npm run dev",
    "dev:electron": "wait-on -t 30000 tcp:3000 && electron .",
    "compile": "tsc",
    "build:nextjs": "node scripts/build-nextjs.mjs",
    "build": "npm run build:nextjs && tsc && electron-builder --config electron-builder.yml",
    "release": "npm run build:nextjs && tsc && electron-builder --config electron-builder.release.yml --publish always",
    "release:esigner": "npm run build:nextjs && tsc && electron-builder --config electron-builder.esigner.yml --publish always",
    "start": "electron ."
  },
  "dependencies": {
    "electron-updater": "6.8.9"
  },
  "overrides": {
    "js-yaml": "4.3.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "electron": "^44.2.0",
    "electron-builder": "26.7.0",
    "typescript": "^5.7.0",
    "wait-on": "^8.0.0"
  }
}
